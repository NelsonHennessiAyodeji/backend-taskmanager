# Task Management REST API

A RESTful backend API for a simple Task Management System. The API allows team members to create, retrieve, filter, update, and delete tasks.

This project was developed as part of a Backend Intern Technical Case Study.

---

## Features

- Create tasks
- Retrieve all tasks
- Retrieve a single task by ID
- Update existing tasks
- Delete tasks
- Filter tasks by status
- Input validation
- MongoDB database persistence
- Mongoose ODM
- Appropriate HTTP status codes
- Centralized error handling
- Handling of invalid task IDs
- Handling of non-existent task IDs
- Handling of non-existent routes
- RESTful API structure

---

## Technology Stack

- **Node.js** — JavaScript runtime
- **TypeScript** — Static typing and compile-time type checking
- **Express.js** — REST API framework
- **MongoDB** — Database
- **Mongoose** — MongoDB ODM
- **tsx** — TypeScript development/runtime tool
- **dotenv** — Environment variable management

---

## Project Structure

```text
src/
├── controllers/
│   └── taskController.ts
│
├── models/
│   └── taskModel.ts
│
├── routes/
│   └── taskRoutes.ts
│
├── middleware/
│   ├── errorHandler.ts
│   └── notFound.ts
│
├── config/
│   └── db.ts
│
└── index.ts

.env
.gitignore
package.json
tsconfig.json
README.md
```

> The exact folder and file names may vary slightly depending on the implementation.

---

# Getting Started

## Prerequisites

Make sure the following are installed:

- Node.js
- npm
- A MongoDB database, either locally or through MongoDB Atlas

---

## Installation

Clone the repository:

```bash
git clone <YOUR-GITHUB-REPOSITORY-URL>
```

Navigate into the project:

```bash
cd <PROJECT-DIRECTORY>
```

Install dependencies:

```bash
npm install
```

---

# Environment Variables

Create a `.env` file in the root of the project.

```env
PORT=3000
MONGO_URI=your_mongodb_connection_string
```

Example:

```env
PORT=3000
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/task-manager
```

The `.env` file contains sensitive configuration and should not be committed to GitHub.

The following should be included in `.gitignore`:

```text
.env
node_modules/
dist/
```

---

# Running the Application

## Development

Run the application in development mode:

```bash
npm run dev
```

The API will be available at:

```text
http://localhost:3000
```

## Production Build

Compile the TypeScript source:

```bash
npm run build
```

Start the compiled application:

```bash
npm start
```

---

# API Documentation

## Base URL

```text
http://localhost:3000
```

The API uses JSON for request and response bodies.

---

# Task Object

A task contains the following information:

```json
{
  "id": "task-id",
  "title": "Learn MongoDB",
  "description": "Study MongoDB queries and Mongoose",
  "status": "Pending",
  "dueDate": "2026-08-20T18:00:00.000Z",
  "createdAt": "2026-08-13T12:00:00.000Z"
}
```

## Fields

| Field         | Type        | Description                                     |
| ------------- | ----------- | ----------------------------------------------- |
| `id`          | String      | Unique identifier generated by MongoDB/Mongoose |
| `title`       | String      | Task title                                      |
| `description` | String      | Description of the task                         |
| `status`      | String      | Current task status                             |
| `dueDate`     | Date/String | Date and time the task is due                   |
| `createdAt`   | Date        | Date and time the task was created              |

## Status Values

The API supports the following task statuses:

```text
Pending
Completed
```

New tasks default to:

```text
Pending
```

---

# API Endpoints

| Method   | Endpoint                  | Description              |
| -------- | ------------------------- | ------------------------ |
| `POST`   | `/tasks`                  | Create a new task        |
| `GET`    | `/tasks`                  | Retrieve all tasks       |
| `GET`    | `/tasks?status=Pending`   | Retrieve pending tasks   |
| `GET`    | `/tasks?status=Completed` | Retrieve completed tasks |
| `GET`    | `/tasks/:id`              | Retrieve one task        |
| `PATCH`  | `/tasks/:id`              | Update a task            |
| `DELETE` | `/tasks/:id`              | Delete a task            |

---

# 1. Create a Task

## POST `/tasks`

Creates a new task.

### Request

```http
POST /tasks
Content-Type: application/json
```

### Request Body

```json
{
  "title": "Learn MongoDB",
  "description": "Study MongoDB queries and Mongoose",
  "dueDate": "2026-08-20T18:00:00.000Z"
}
```

A status can also be supplied:

```json
{
  "title": "Learn MongoDB",
  "description": "Study MongoDB queries and Mongoose",
  "status": "Pending",
  "dueDate": "2026-08-20T18:00:00.000Z"
}
```

### Successful Response

**201 Created**

```json
{
  "msg": "Created new task",
  "task": {
    "id": "...",
    "title": "Learn MongoDB",
    "description": "Study MongoDB queries and Mongoose",
    "status": "Pending",
    "dueDate": "2026-08-20T18:00:00.000Z",
    "createdAt": "2026-08-13T12:00:00.000Z"
  }
}
```

### Possible Status Codes

| Status | Meaning                   |
| ------ | ------------------------- |
| `201`  | Task successfully created |
| `400`  | Invalid request data      |
| `500`  | Internal server error     |

---

# 2. Get All Tasks

## GET `/tasks`

Retrieves all tasks.

### Request

```http
GET /tasks
```

### Successful Response

**200 OK**

```json
[
  {
    "id": "...",
    "title": "Learn MongoDB",
    "description": "Study MongoDB queries",
    "status": "Pending",
    "dueDate": "2026-08-20T18:00:00.000Z",
    "createdAt": "2026-08-13T12:00:00.000Z"
  },
  {
    "id": "...",
    "title": "Build REST API",
    "description": "Create the task management API",
    "status": "Completed",
    "dueDate": "2026-08-18T18:00:00.000Z",
    "createdAt": "2026-08-12T10:00:00.000Z"
  }
]
```

---

# 3. Filter Tasks by Status

## GET `/tasks?status=<status>`

Tasks can be filtered by status using a query parameter.

### Get Pending Tasks

```http
GET /tasks?status=Pending
```

### Get Completed Tasks

```http
GET /tasks?status=Completed
```

The API validates the supplied status before using it as a database filter.

### Invalid Status

For example:

```http
GET /tasks?status=Invalid
```

The API returns:

**400 Bad Request**

```json
{
  "msg": "Invalid status"
}
```

### Successful Response

**200 OK**

```json
[
  {
    "id": "...",
    "title": "Learn MongoDB",
    "description": "Study MongoDB queries",
    "status": "Pending",
    "dueDate": "2026-08-20T18:00:00.000Z",
    "createdAt": "2026-08-13T12:00:00.000Z"
  }
]
```

---

# 4. Get a Single Task

## GET `/tasks/:id`

Retrieves a specific task using its MongoDB ID.

### Request

```http
GET /tasks/64f123456789abcdef123456
```

### Successful Response

**200 OK**

```json
{
  "id": "64f123456789abcdef123456",
  "title": "Learn MongoDB",
  "description": "Study MongoDB queries",
  "status": "Pending",
  "dueDate": "2026-08-20T18:00:00.000Z",
  "createdAt": "2026-08-13T12:00:00.000Z"
}
```

### Invalid ID

If an invalid MongoDB ID is supplied:

```http
GET /tasks/123
```

the API returns:

**400 Bad Request**

```json
{
  "msg": "Invalid task ID"
}
```

### Non-existent ID

If the ID has a valid MongoDB format but does not correspond to an existing task:

**404 Not Found**

```json
{
  "msg": "Task not found"
}
```

---

# 5. Update a Task

## PATCH `/tasks/:id`

Updates one or more properties of an existing task.

### Request

```http
PATCH /tasks/64f123456789abcdef123456
Content-Type: application/json
```

### Update Status

```json
{
  "status": "Completed"
}
```

### Update Multiple Fields

```json
{
  "title": "Learn Advanced MongoDB",
  "description": "Study advanced MongoDB queries",
  "status": "Completed",
  "dueDate": "2026-08-25T18:00:00.000Z"
}
```

### Successful Response

**200 OK**

```json
{
  "msg": "Task updated successfully",
  "task": {
    "id": "64f123456789abcdef123456",
    "title": "Learn Advanced MongoDB",
    "description": "Study advanced MongoDB queries",
    "status": "Completed",
    "dueDate": "2026-08-25T18:00:00.000Z",
    "createdAt": "2026-08-13T12:00:00.000Z"
  }
}
```

### Possible Status Codes

| Status | Meaning                         |
| ------ | ------------------------------- |
| `200`  | Task successfully updated       |
| `400`  | Invalid task ID or request data |
| `404`  | Task does not exist             |
| `500`  | Internal server error           |

---

# 6. Delete a Task

## DELETE `/tasks/:id`

Deletes a task using its ID.

### Request

```http
DELETE /tasks/64f123456789abcdef123456
```

### Successful Response

**200 OK**

```json
{
  "msg": "Task deleted successfully"
}
```

### Possible Status Codes

| Status | Meaning                   |
| ------ | ------------------------- |
| `200`  | Task successfully deleted |
| `400`  | Invalid task ID           |
| `404`  | Task does not exist       |
| `500`  | Internal server error     |

---

# HTTP Status Codes

The API uses standard HTTP status codes to communicate the result of each request.

| Code  | Meaning               | Usage                                       |
| ----- | --------------------- | ------------------------------------------- |
| `200` | OK                    | Successful GET, PATCH, or DELETE request    |
| `201` | Created               | Successfully created a task                 |
| `400` | Bad Request           | Invalid input, status, or malformed task ID |
| `404` | Not Found             | Requested task or route does not exist      |
| `500` | Internal Server Error | Unexpected server/database error            |

---

# Error Handling

The application uses centralized error-handling middleware so controllers do not need to repeat the same generic error-response logic.

Errors are handled according to their type.

```text
Invalid input
    ↓
400 Bad Request

Invalid MongoDB ID
    ↓
400 Bad Request

Valid ID but task does not exist
    ↓
404 Not Found

Unexpected server/database error
    ↓
500 Internal Server Error
```

A separate fallback route handles requests to endpoints that do not exist.

For example:

```http
GET /something-that-does-not-exist
```

returns:

```json
{
  "msg": "Route not found"
}
```

---

# Input Validation

Incoming task data is validated before being accepted by the database.

Validation includes:

- `title` is required
- `title` has a minimum length
- `title` has a maximum length
- `description` is required
- `status` must be one of the supported values
- `dueDate` must contain a valid date value
- Task IDs must be valid MongoDB ObjectIds when used in ID-based endpoints

This prevents malformed data from being stored in the database.

---

# Database

MongoDB is used as the persistent database for the application.

Mongoose is used as the ODM (Object Data Modeling) layer between the Node.js application and MongoDB.

Mongoose provides:

- Schema definitions
- Data validation
- MongoDB model abstraction
- Query functionality
- Automatic timestamps

MongoDB/Mongoose automatically generates a unique `_id` for each document. This identifier is used when retrieving, updating, and deleting individual tasks.

The application does not manually generate task IDs.

The `createdAt` timestamp is also generated automatically through the Mongoose timestamp configuration.

---

# REST API Design

The API follows REST-oriented conventions.

| HTTP Method | Endpoint     | Purpose                 |
| ----------- | ------------ | ----------------------- |
| `POST`      | `/tasks`     | Create a task           |
| `GET`       | `/tasks`     | Retrieve all tasks      |
| `GET`       | `/tasks/:id` | Retrieve one task       |
| `PATCH`     | `/tasks/:id` | Partially update a task |
| `DELETE`    | `/tasks/:id` | Delete a task           |

Filtering is implemented through query parameters:

```http
GET /tasks?status=Pending
```

Rather than creating separate endpoints such as:

```text
/tasks/pending
/tasks/completed
```

This keeps the API flexible and follows common REST API conventions.

---

# Example API Usage

The API can be tested with cURL, Postman, Insomnia, or another HTTP client.

## Create a task

```bash
curl -X POST http://localhost:3000/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Complete backend challenge",
    "description": "Finish the Task Management REST API",
    "dueDate": "2026-08-20T18:00:00.000Z"
  }'
```

## Get all tasks

```bash
curl http://localhost:3000/tasks
```

## Get pending tasks

```bash
curl "http://localhost:3000/tasks?status=Pending"
```

## Get completed tasks

```bash
curl "http://localhost:3000/tasks?status=Completed"
```

## Get a specific task

```bash
curl http://localhost:3000/tasks/TASK_ID
```

## Update a task

```bash
curl -X PATCH http://localhost:3000/tasks/TASK_ID \
  -H "Content-Type: application/json" \
  -d '{
    "status": "Completed"
  }'
```

## Delete a task

```bash
curl -X DELETE http://localhost:3000/tasks/TASK_ID
```

---

# Assumptions

The following assumptions were made during development:

1. A task has two possible statuses: `Pending` and `Completed`.
2. New tasks default to `Pending`.
3. MongoDB/Mongoose generates the unique task ID automatically.
4. `createdAt` is generated automatically by Mongoose timestamps.
5. Task IDs are MongoDB ObjectIds.
6. The API accepts and returns JSON.
7. Filtering is currently supported by task status.
8. Authentication and authorization are outside the scope of this case study because they were not specified in the requirements.
9. A task can be partially updated using the `PATCH` endpoint.
10. Invalid task IDs and valid but non-existent task IDs are treated as different error cases.

---

# Challenges Encountered

## Query Parameter Validation

Query parameters received through Express are not automatically typed as the application's domain types.

For example, the `status` query parameter has a broad Express/Node query type rather than automatically being recognized as:

```text
"Pending" | "Completed"
```

The API therefore validates the supplied value before using it as a MongoDB filter.

---

## MongoDB ID Validation

MongoDB IDs need to be validated before performing database operations.

This allows the API to distinguish between:

- An incorrectly formatted ID
- A correctly formatted ID that does not exist in the database

This results in clearer and more appropriate HTTP responses.

---

## Error Handling

A centralized error-handling middleware was used to keep controllers focused on their primary responsibilities.

Rather than repeating generic error handling in every controller, errors can be passed to a common handler which determines the appropriate HTTP response.

---

## TypeScript Integration

TypeScript was used throughout the application to provide compile-time type checking for controllers, requests, database-related code, and application logic.

This helped catch invalid assumptions and type mismatches during development rather than only at runtime.

---

# What I Would Improve With More Time

If more development time were available, I would consider adding:

- Pagination for `GET /tasks`
- More advanced filtering
- Sorting
- Search functionality
- Authentication and authorization
- User accounts and task ownership
- Automated unit tests
- Automated integration tests
- API documentation using Swagger/OpenAPI
- Request logging
- More comprehensive request validation
- Docker support
- Production-oriented configuration
- Rate limiting
- CI/CD using GitHub Actions
- More robust database connection/reconnection handling
- Improved API response consistency

---

# Testing

The API can be tested using:

- Postman
- Insomnia
- cURL
- VS Code REST Client

The primary functionality to test includes:

```text
POST   /tasks
GET    /tasks
GET    /tasks?status=Pending
GET    /tasks?status=Completed
GET    /tasks/:id
PATCH  /tasks/:id
DELETE /tasks/:id
```

Error cases should also be tested:

```text
Invalid task data
Invalid task ID
Non-existent task ID
Invalid status
Non-existent route
Database errors
```

---

# Technical Decisions

## Why TypeScript?

TypeScript was chosen to provide static typing and better maintainability. It also helps identify type-related errors during development before the application is executed.

## Why Express?

Express provides a lightweight and well-established way to build REST APIs with Node.js. It also provides straightforward middleware and routing functionality.

## Why MongoDB?

MongoDB provides a document-oriented data model that maps naturally to the task object used by this application. It also integrates well with Node.js through Mongoose.

## Why Mongoose?

Mongoose provides schema definitions, validation, models, and convenient database operations while still allowing direct use of MongoDB's document-oriented model.

---

# Future Improvements

Possible future versions of the API could introduce:

```text
Authentication
      ↓
Users
      ↓
Task ownership
      ↓
Authorization
      ↓
Advanced filtering/search
      ↓
Pagination
      ↓
Automated testing
      ↓
API documentation
      ↓
CI/CD
      ↓
Production deployment
```

---

# Author

**Ayodeji Nelson**

Backend Developer

---

# Internship Case Study

This project was developed as part of a Backend Intern Technical Case Study.

The primary goals of the implementation were to demonstrate:

- REST API design
- Database integration
- Input validation
- HTTP status code usage
- Error handling
- Code organization
- TypeScript usage
- API documentation
